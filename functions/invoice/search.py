def by_transaction(transaction_id, client_email=None):
    #TODO: buscar nota fiscal no banco de dados pelo transaction_id e enviar para o email do cliente
    if client_email is None:
        return #NÃO ENVIAR EMAIL
    return None